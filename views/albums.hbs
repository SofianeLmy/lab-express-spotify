<ul>

  {{#each albums}}

    <li>

      <h4>{{name}}</h4>
      {{#if images.length}}

        <img src='{{images.0.url}}' alt='{{name}}' />
      {{/if}}

      <a href="/album/{{id}}">View tracks</a>
    </li>

  {{/each}}

</ul>